# Power BI Assets

The `.pbix` files in this folder are transparent placeholders generated by automation.
Open `data/sample_metrics.csv` in Power BI Desktop, build the visuals, and replace the placeholders with the real artifacts.